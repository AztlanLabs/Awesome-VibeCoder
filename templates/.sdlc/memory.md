# Memory

<!--
Append-only cross-session chronicle. Any agent may write. Never edit or
delete prior entries. This is a side effect of verified work, not the
deliverable — do not treat an entry here as equivalent to completing a task.
Format:

### [ISO-8601 Timestamp] — [Agent Role]
[Real artifact paths changed + verification command/result, not a prose retelling]
-->

### <ISO-8601> — <Role>

Workspace scaffolded via the sdlc-shared-memory skill. No work started yet.
